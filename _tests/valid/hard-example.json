{
	"the": {
		"test_string": { "type": "string", "value": "You'll hate me after this - #" },
		
		"hard": {
			"test_array": [
				{ "type": "string", "value": "] " },
				{ "type": "string", "value": " # " }
			],
			"test_array2": [
				{ "type": "string", "value": "Test #11 ]proved that" },
				{ "type": "string", "value": "Experiment #9 was a success" }
			],
			"another_test_string": { "type": "string", "value": " Same thing, but with a string #" },
			"harder_test_string": { "type": "string", "value": " And when \"'s are in the string, along with # \"" },
			
			"bit#": {
				"what?": { "type": "string", "value": "You don't think some user won't do that?" },
				"multi_line_array": [ { "type": "string", "value": "]" } ]
			}
		}
	}
}
